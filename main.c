#include "holberton.h"

/**
 * main - Call all functions ($ ) and splits a string
 *
 * Return: ....
 */

int main(void)
{
	_prompt();
	
	/*
	else if (isatty(STDIN_FILENO) == 0)
	{
		printf("%s, %d\n", *argv, argc);
		if (execve(*argv, argv, NULL) == -1)
                {
                        perror("Execve Error");
                }
	}
	*/
	return (0);
}
